package chap16_collectionFramework;

import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

import chap12_nestedclass.clazz.Car;

public class _07_iterator {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//Map<String
		
		
		List<Car> cList = new ArrayList<car>();
		
		Car car = new Car("현대", "제네시스", "은색", 5000);
		cList.add(car);
		car = 
				Car car = new Car("현대", "아반떼", "흰색", 3000);
		cList.add(car);
		car = 
				Car car = new Car("현대", "소나타", "검정색", 4000);
		cList.add(car);
		car = 
				Car car = new Car("기아", "ev6", "파란색", 4000);
		cList.add(car);
		car = 
				Car car = new Car("기아", "모하비", "빨간색", 5000);
		cList.add(car);
		car = 
		
				syso
				
				//Iterator로 요소들 받아오기
				Iterator<E> it = cList.iterator();
		
		while(it.hasNext()) {
			(Car)it.next();
			c.carInfo();
			if(c.company.equals("현대"))
				it.remove();
		}
		System.out.println(cList.size());
		
	}

}
