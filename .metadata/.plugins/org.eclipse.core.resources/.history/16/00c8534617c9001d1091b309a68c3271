package Homework;

import java.util.Scanner;

public class _0322_middle {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

//		1. Reverse라는 클래스를 만들고 메소드로 
//		   String reverseString(String str)를 만듭니다.
//		   매개변수로 받은 str을 거꾸로 만든 String을 리턴하도록 만드세요.

//		class Reverse(str){
//			
//			
//			
//		}
//		
		
		
		
//		2. 사용자가 입력한 문자열에서 중복된 문자를 제거한 문자열을 출력하세요.

		Scanner sc = new Scanner(System.in);
		String str = sc.nextLine();
		
		
		//두번째부터는 공백으로 처리...?
		String a = "";
		for(int i = 0; i < str.length(); i++) {    //str.indexOf(str)은 str의 배열에서 처음 검색되는 위치를 리턴
			str.charAt(i);							//그래서 str.indexOf(str.charAt(i))가 배열의 위치 i와 같다면 중복 없음
													//같지 않다면 이미 앞에 동일한 문자가 있다는 것.
			if(str.indexOf(str.charAt(i)) == i) {  
			a += str.charAt(i);					//char형태를 다시 ""를 추가하여 문자열 형태로 변환
			}else {
				System.out.println(str.replace(str.charAt(i), ""));
			}
			System.out.println(a);
		}
		
		
		
//		3. 사용자가 입력한 문자열에서 3의 배수 자리의 문자는 3으로 
//		   5의 배수 자리의 문자는 5로 변경하여 출력하세요.
		
		//배열로 
		
//		Scanner sc = new Scanner(System.in);
//		String str = sc.nextLine();
//		
//		for(int i = 0; i < str.length(); i++) {
//			if((i+1)%3 == 0) {
//				str.replace[i] = "3";
//			}else if((i+1)%5 == 0) {
//				
//			}else {
//				
//			}
//			
//		}
		
		
		
		
		
		
		
		
		
	}

}
